# OCR inference backend (vLLM OpenAI-compatible). Start separately on a GPU host.
OCR_STUB_MODE=1
OCR_INFERENCE_URL=
OCR_INFERENCE_MODEL=Unlimited-OCR
OCR_INFERENCE_API_KEY=
OCR_SERVICE_API_KEY=
OCR_HOST=0.0.0.0
OCR_PORT=8100
OCR_PDF_DPI=300
OCR_DEFAULT_IMAGE_MODE=gundam
