import { MarketingPage } from '../components/MarketingPage';

export default function Page() {
  return (
    <MarketingPage
      eyebrow="Security"
      title="Enterprise-ready security for AI operations"
      description="SSO, SCIM, Entra ID, LDAP, SAML, OAuth, MFA, secrets, RBAC/ABAC, and full audit — designed for regulated and sovereign deployments."
    />
  );
}
