import { MarketingPage } from '../components/MarketingPage';

export default function Page() {
  return (
    <MarketingPage
      eyebrow="Developers"
      title="Developer platform for enterprise AI"
      description="API, SDK, CLI, examples, plugins, and marketplace publishing on the AI-Pass infrastructure layer."
    />
  );
}
